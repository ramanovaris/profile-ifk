# Base path prefix when deploying behind a subpath (e.g. https://example.com/myapp/).
# Leave empty for root deployment (https://example.com/) or local dev on localhost.
# When set, trailingSlash and images.unoptimized auto-enable (workarounds for dev deploy).
NEXT_PUBLIC_BASE_PATH=
